const pages = [
  { path: "/", groups: ["Developer admin", "admin"] },
  { path: "shipments", groups: ["Developer admin", "admin"] },
  { path: "/shipments/:waybillNumber", groups: ["Developer admin", "admin"] },
];

export default pages;
